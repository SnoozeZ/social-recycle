<% provide(:title, @user.username) %>
<div class="container-fluid">
  <div class="row">
    <div class="col-xs-6" style = text-align:center>
      <h1><%= @user.username %></h1>
      <p><span>Name: </span><%= @user.username %></p>
      <p><span>Reg at: </span><%= @user.reg_time %>
      <p><span>phone: </span><%= @user.phone %></p>
    </div>
  </div>
  <div class="container" style="margin-top:20px;">
    <ul class="nav nav-tabs" id="usertabs">
      <li class="active" ><a data-toggle="tab" href="#dropped">Dropped Items</a></li>
      <li><a data-toggle="tab" href="#dibs2">Dibs Items</a></li>
    </ul>
    <div id="tabcontent" class="tab-content">
      <div id="dropped" class="tab-pane fade in active">
        <% @items.each do |s| %>

            <p><a href= "../items/<%= s.id %>"><%= s.title %></a></p>
        <%end%>

      </div>
      <div id="dibs2" class="tab-pane fade">
        <h1>Dibs items</h1>
        <p>das;fjadsl;kfjsadklfjadsklfmlkadsvml;kvml;c
        adsfl;jasdfk;fk'adsfkasd</p>
      </div>

    </div>
    </div>
  </div>
</div>

